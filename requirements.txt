# To install all Python packages, copy and run this command in your terminal:
# pip install discord.py python-dotenv selenium loguru keyboard mutagen yt-dlp spotipy
#
# --- Other System Dependencies ---
# These are required by the bot but cannot be installed via pip.
# You must install them on the host machine and add them to your system's PATH.
#
# Deno:   Required by yt-dlp for music. Install from [deno.land/install]
# FFmpeg: Required for all music playback. Install from [ffmpeg.org/download.html]
# MS Edge: Required by Selenium to run the browser.
#
# --- Python Packages (pip) ---

discord.py
python-dotenv
selenium
loguru
keyboard
mutagen
yt-dlp
spotipy
